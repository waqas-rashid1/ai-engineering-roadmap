# Step 0 — API smoke test
anthropic>=0.40.0
python-dotenv>=1.0.0

# Step 1 — document loading
pypdf>=4.0.0
#
# Tip: sentence-transformers pulls in PyTorch (~2GB). If install fails,
# use CPU-only PyTorch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install chromadb sentence-transformers pypdf streamlit
